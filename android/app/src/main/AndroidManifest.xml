<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <!-- Vincula la app con el dominio (junto a /.well-known/assetlinks.json) -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:label="@string/app_name">

            <!-- La app se identifica ante la web con su propia version.
                 Una TWA renderiza con Chrome, asi que del lado del sitio es
                 indistinguible de una pestana normal: sin estos parametros no
                 hay manera de saber si el telefono trae un APK viejo. app.js
                 los guarda al abrir y con eso decide si avisar "actualiza".
                 Los valores los inyecta build.gradle (manifestPlaceholders),
                 no se escriben a mano: asi no se desincronizan. -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://nexuspro.cmtelecommgt.com/?app=android&amp;appvc=${appVersionCode}&amp;appvn=${appVersionName}" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/colorNav" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="nexuspro.cmtelecommgt.com" />
            </intent-filter>
        </activity>
    </application>
</manifest>
