# Compilación
.gradle/
build/
app/build/
local.properties

# 🔐 FIRMA — NUNCA al repositorio (respáldalos aparte: sin el keystore
# no se pueden publicar actualizaciones de la app)
#
# Van por PATRÓN, no por nombre. Antes decía sólo `tallerpro.keystore`, así que
# al rotar la llave el 2026-08-25 el keystore nuevo (nexuspro-2026.keystore) no
# quedaba cubierto y `git add -A` se lo llevaba al repositorio. Se detectó al
# revisar el staging, antes de confirmar. Cualquier llave futura ya está cubierta.
*.keystore
*.jks
*.p12
keystore.properties
